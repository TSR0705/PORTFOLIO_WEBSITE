import { Project } from "@/lib/projects";
import { ProjectTheme } from "@/lib/project-design";

export default function WhoIAmCaseStudy({ project, theme }: { project: Project; theme: ProjectTheme }) {
  return null;
}
