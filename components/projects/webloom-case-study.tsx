import { Project } from "@/lib/projects";
import { ProjectTheme } from "@/lib/project-design";

export default function WebloomCaseStudy({ project, theme }: { project: Project; theme: ProjectTheme }) {
  return null;
}
