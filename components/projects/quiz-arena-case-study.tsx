import { Project } from "@/lib/projects";
import { ProjectTheme } from "@/lib/project-design";

export default function QuizArenaCaseStudy({ project, theme }: { project: Project; theme: ProjectTheme }) {
  return null;
}
