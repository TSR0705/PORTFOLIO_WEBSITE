import { Project } from "@/lib/projects";
import { ProjectTheme } from "@/lib/project-design";

export default function SmartTabOrganizerCaseStudy({ project, theme }: { project: Project; theme: ProjectTheme }) {
  return null;
}
